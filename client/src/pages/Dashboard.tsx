import Logout from "../components/Logout";

export default function Dashboard() {
    return (
        <div>
            <h1>
                Welcome to SoundStack
            </h1>

            <Logout />
        </div>
    );
}